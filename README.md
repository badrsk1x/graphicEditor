App to draw geometric shapes

\$shapes = [

['type' => 'circle', 'params' => [...]],

['type' => 'circle', 'params' => [...]] ];

It makes all calculations and saves result to image(binary file).

## Install

`composer install`
